server:
  port: 8085
spring:
  datasource:
    url: "jdbc:mysql://127.0.0.1:3306/gituserdb"
    username: root
    password: root
    ##root1234
    ##driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true

  application:
    name: app1

eureka:
  client:
    register-with-eureka: true
  service-url: http://localhost:8761/eureka/
